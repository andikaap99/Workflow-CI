name: coffee_sales_forecasting

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir:
        type: str
        default: "coffee_sales_preprocessing"
    command: "python modelling.py --data_dir {data_dir}"